#include<iostream>
using namespace std;
int main(){
    float length,width,area;
    cout<<"\n Enter length & width of the rectangle: ";
    cin>>length>>width;
    area=length * width;
    cout<<"\n Area of rectangle = "<<area;
}